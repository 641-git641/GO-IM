{
    "gateway": {
        "http_addr": ":8080",
        "tcp_addr": ":18083",
        "transport": "both",
        "heartbeat": "30s",
        "heartbeat_fail": 3,
        "check_origin": [],

        "conn": {
            "pong_wait": "60s",
            "ping_period": "54s",
            "max_msg_size": 65536,
            "send_buf_size": 256,
            "offline_max_size": 1000
        },

        "rate_limit": {
            "enabled": false,
            "rate": 10,
            "burst": 20,
            "cleanup_interval": "5m"
        },

        "redis": {
            "addr": "127.0.0.1:6379",
            "password": "",
            "db": 0
        },

        "gnet": {
            "num_event_loops": 0,
            "worker_pool_size": 0
        },

        "mysql": {
            "enabled": true,
            "dsn": "im:im-dev@tcp(127.0.0.1:3307)/im?parseTime=true&charset=utf8mb4"
        },

        "auth": {
            "dev_mode": true
        },

        "kafka": {
            "enabled": true,
            "brokers": ["localhost:9094"],
            "topic": "im.message.persist"
        },

        "logic": {
            "addr": "localhost:50051",
            "listen_addr": ":50051"
        },

        "grpc": {
            "addr": "",
            "node_id": "",
            "peer_addrs": {}
        },

        "object_storage": {
            "enabled": true,
            "endpoint": "127.0.0.1:9000",
            "access_key": "minioadmin",
            "secret_key": "minioadmin",
            "bucket": "im-files",
            "use_ssl": false,
            "max_upload": 10485760
        },

        "recall_window_ms": 120000,
        "history_default_limit": 30,
        "search_default_limit": 20,
        "dedup_ttl": "5m",
        "persist_concurrency": 64
    },

    "admin_uids": [],

    "logic": {
        "addr": "localhost:50051",
        "listen_addr": ":50051",
        "mysql": { "enabled": true, "dsn": "im:im-dev@tcp(127.0.0.1:3307)/im?parseTime=true&charset=utf8mb4" },
        "kafka": { "enabled": true, "brokers": ["localhost:9094"], "topic": "im.message.persist" },
        "worker_id": 2
    },

    "stability": {
        "max_connections": 0,
        "http_read_timeout": "10s",
        "http_write_timeout": "10s",
        "http_idle_timeout": "120s",
        "shutdown_timeout": "30s",
        "pprof_enabled": true,
        "pprof_addr": "localhost:6060"
    },

    "jwt": {
        "secret": "change-me-in-production",
        "expiration": "168h"
    },

    "snowflake": {
        "worker_id": 1
    }
}
